import { HospitalAppointmentDetails } from './hospital-appointment-details';

describe('HospitalAppointmentDetails', () => {
  it('should create an instance', () => {
    expect(new HospitalAppointmentDetails()).toBeTruthy();
  });
});
